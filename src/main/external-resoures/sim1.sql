create table sim1(
   id INT NOT NULL AUTO_INCREMENT,
   ts TIMESTAMP NOT NULL,
   cpu FLOAT NOT NULL,
   mem FLOAT NOT NULL,
   PRIMARY KEY ( id )
);
create table sim2(
   id INT NOT NULL AUTO_INCREMENT,
   ts TIMESTAMP NOT NULL,
   cpu FLOAT NOT NULL,
   mem FLOAT NOT NULL,
   PRIMARY KEY ( id )
);
create table sim3(
   id INT NOT NULL AUTO_INCREMENT,
   ts TIMESTAMP NOT NULL,
   cpu FLOAT NOT NULL,
   mem FLOAT NOT NULL,
   PRIMARY KEY ( id )
);